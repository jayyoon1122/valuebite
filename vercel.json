{
  "buildCommand": "cd apps/web && npx next build",
  "installCommand": "rm -rf apps/mobile apps/api apps/worker && npm install --legacy-peer-deps",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next"
}
