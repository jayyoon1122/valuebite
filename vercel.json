{
  "buildCommand": "cd apps/web && npx next build",
  "installCommand": "rm -rf apps/mobile apps/api apps/worker && npm install --force",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next"
}
