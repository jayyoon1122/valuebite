{
  "buildCommand": "cd apps/web && npx next build",
  "installCommand": "npm ci --legacy-peer-deps --include=optional || npm install --legacy-peer-deps --force",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next"
}
