'use client';

/**
 * Price alert banner — will show real price change notifications
 * when price tracking is implemented via Supabase.
 * Currently hidden (no alerts to show).
 */
export function PriceAlertBanner() {
  return null;
}
